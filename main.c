#include <stdio.h> 

typedef struct Node {
    int pswd; 
    struct Node * next; 
}

int main(void) 
{
    int choice, id; 
    
    switch (choice) {
        case 1: //password checker
        case 2: //List previous passwords 
        case 3: //Exit 
    }

    return 0; 
}